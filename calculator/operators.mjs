export const sum = (a, b) => Math.round((a + b) * 1000) / 1000,
  subs = (a, b) => Math.round((a - b) * 1000) / 1000,
  mult = (a, b) => Math.round(a * b * 1000) / 1000,
  div = (a, b) => Math.round((a / b) * 1000) / 1000;
